# Carpeta `config`

En esta carpeta se almacenarán los archivos de configuración de la aplicación, como variables de entorno, configuración de la base de datos y otros parámetros necesarios para el funcionamiento del proyecto.

Por ejemplo: un archivo `database.ts` donde se define la conexión a MongoDB, o un archivo `env.ts` para cargar y gestionar las variables de entorno.
